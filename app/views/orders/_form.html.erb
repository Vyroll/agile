<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shipping_id %>
    <%= f.text_field :shipping_id %>
  </div>

  <div class="field">
    <%= f.label :status.name %>
    <%= f.text_field :status.name %>
  </div>

  <div class="field">
    <%= f.label :user.email %>
    <%= f.text_field :user.email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
